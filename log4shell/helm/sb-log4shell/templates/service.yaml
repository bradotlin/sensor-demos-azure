apiVersion: v1
kind: Service
metadata:
  name: {{ include "sb-log4shell.fullname" . }}
  labels:
    {{- include "sb-log4shell.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sb-log4shell.selectorLabels" . | nindent 4 }}
